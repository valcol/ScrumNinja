module.exports = function() {
   this.When(/^I create a sprint$/, function () {
    //browser.waitForExist('', 2000);
    //browser.setValue('',);

    //submit
    browser.waitForExist('', 2000);
    browser.click('');
  });
};
